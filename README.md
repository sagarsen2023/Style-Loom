# Features to implement
- in `user/homepage`:
1. In `user/cart` while clicking on `checkout` the cart array of user will be empty and for every id of the product the quantity will be decreased accordingly.